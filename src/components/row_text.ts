import styled from 'styled-components/macro';

const Text = styled.span`
    font-weight: bold;
    font-size: ${15 / 14}rem;
    line-height: ${21 / 14}rem;
    margin: ${11 / 14}rem 0;
`;

export default Text;
